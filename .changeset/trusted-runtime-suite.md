---
"react-router-hono-server": major
---

Require React 19, React Router 8.3, Vite 8, Hono 4, Node 24, and Node Server 2; follow React Router's runtime-specific default server entries and optional future flags; validate packed-package consumers across supported runtimes; adopt the current Cloudflare Vite integration and unconditional `RouterContextProvider` load contexts; and remove the maintained example projects in favor of executable documentation and fixtures.
