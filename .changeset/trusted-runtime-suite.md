---
"react-router-hono-server": major
---

Require React 19, React Router 8, Vite 8, Hono 4, Node 24, and Node Server 2; validate packed-package consumers across supported runtimes; adopt the current Cloudflare Vite integration and unconditional `RouterContextProvider` load contexts; and remove the maintained example projects in favor of executable documentation and fixtures.
